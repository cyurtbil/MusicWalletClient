'use strict';

app.controller('HomeController', ['$http', 'ServerUrl', '$scope', function($http, ServerUrl, $scope) {

  // $http.get(ServerUrl + 'cloud/get_current_user').success(function(response) {
  //   console.log(response);
  // });
}]);